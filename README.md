Clone the repository to your suitable directory.<br/>

Go to the project directory as: <br/>
cd ap-backend-master


## Install dependencies
npm install

## Run the server
npm start

## Server runs on http://localhost:5000
